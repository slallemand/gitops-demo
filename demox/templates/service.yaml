apiVersion: v1
kind: Service
metadata:
  name: {{ include "demox.fullname" . }}
  labels:
    {{- include "demox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demox.selectorLabels" . | nindent 4 }}
